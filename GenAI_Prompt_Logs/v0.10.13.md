# User Said:

v0.10.12 surprisingly did not cause problems during testing. Thank you.

For v0.10.13, can you please implement these changes:

- When the new "Block local file and external app URL schemes" option is enabled, please change the popup message from a browser default, to a SBM themed popup in the same style as the new bookmark / new folder popups. Or if we had another popup in this theme, reuse the popup code? If not, this would be a good time to make a generic popup we can populate with custom text & a custom button text, then use it for this warning.
- When the warning for opening a file/blocked type when the "Block local file and external app URL schemes" option is enabled should not throw a browser level (console) warning. It should at most throw the warning into the (transient) SBM Warnings/Errors log.
- Please move the "Bookmark URL Protections" section from Options -> Advanced to Options -> Safety.

Thanks!

# Chat GPT Said:

Worked for 6m 26s

Done: Download `Simple_Bookmarks_Manager_v0.10.13.zip`

## Files changed

- `manifest.json`
- `CHANGELOG.md`
- `manager.js`
- `manager.css`
- `options.html`
- `help.html`
- `_locales/en/messages.json`

## Summary of changes

- Bumped version to `0.10.13`.
- Replaced native blocked-bookmark URL `alert()` with a reusable SBM-themed message dialog.
- Added generic safe text-only popup helper with custom heading/message/button text.
- Blocked bookmark URL opens now write to SBM’s transient Warnings/Errors log via `addSessionLogRecord()` instead of `console.warn()`.
- Moved **Bookmark URL protections** from Options → Advanced to Options → Safety.
- Updated Help and English locale text to reflect the Safety location and new popup/log behavior.
- Permissions unchanged.

## Validation performed

- `node --check`: passed for all 8 JavaScript files.
- JSON parse: passed for all 22 JSON files.
- Locale JSON parse: passed for all 21 locale files.
- Placeholder preservation check: passed for shared locale keys.
- LF line endings: passed for all detected text files.
- Binary files unchanged.
- ZIP integrity: passed with `zipfile.testzip()` and `unzip -tqq`.
- ZIP structure: files are at ZIP root, no extra parent folder.

## Caveats / testing focus

- Non-English locales were not updated; they now intentionally miss 10 English keys.
- No browser runtime test was performed here.
- Please test strict URL protection with `file:`, `mailto:`, and custom-scheme bookmarks, plus Options → Safety placement and embedded Options display.