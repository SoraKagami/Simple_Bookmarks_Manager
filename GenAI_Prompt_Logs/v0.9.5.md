# User Said:

v0.9.4 Went well, thank you. The only issue was my requested change "When `SideBarMode` is enabled, can you add code so that the current SBM tab closes while SBM is opened in the sidebar? " did not work. If the code added is correct, then we should remove it since SBM does not appear to be able to close tabs without the tab permission.

For v0.9.5:

- Please implement the above fix.
- Middle bookmarks pane: the text label "Bookmarks bar" should be just "Bookmarks".
- When `SideBarMode` is enabled and user starts typing in the search bookmarks text field, please hide the Library pane and show the middle bookmarks pane instead. When the search field is cleared, please hide the middle bookmarks pane and show the Library pane again.

When `SideBarMode` is enabled and the middle bookmarks pane is shown (due to search), the following UI changes should be made to the middle pane:

- Hide the label "Bookmarks" ("Bookmarks bar" before applying above fix).
- Button "Click to Hide Details" should be hidden
- "Visual sort:" and the dropdown should be moved to the left.

Once `SideBarMode` is disabled, the middle bookmarks pane should revert back to the current layout.

Thanks!

# ChatGPT Said:

Worked for 5m 10s

- **Download:** Simple Bookmarks Manager v0.9.5
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `manager.js`, `manager.css`.
- **Changes:**

    - Removed the unreliable Sidebar Mode tab-closing code.
    - No `tabs` permission was added. Chromium documents that most tab manipulation methods do not require it; the permission primarily exposes sensitive tab properties. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)developer.chrome.com](https://developer.chrome.com/docs/extensions/reference/api/tabs?utm_source=chatgpt.com)
    - Changed the default middle-pane root label from **Bookmarks bar** to **Bookmarks**.
    - Narrow Sidebar Mode now switches from Library to the middle bookmarks pane while searching.
    - Clearing search restores the Library pane and tree selection context.
    - Sidebar search hides the breadcrumb label and Details toggle, and left-aligns **Visual sort** with its dropdown.
    - Normal non-sidebar layout remains unchanged.
- **Validation:** All 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 42 text files use LF; all 21 locale files and all binary assets remained unchanged; ZIP integrity and root structure passed.
- **SHA-256:** `63f0a7de4e923648138a9ee139f8c6cb98e0ab3d3315fbf862ebded169de9a99`

### Double-check

- Confirm typing and clearing search swaps panes correctly at different Brave side-panel widths.
- Confirm the middle pane’s horizontal scrolling and bookmark interactions remain usable in the narrow panel.