# User Said:

v0.9.19 Went well, much appreciated!

For v0.9.20:

- Please help me convert the URLs in about.html into plain text. They should not be a hyperlink to conform with the privacy focus of SBM. If the user wants to access those links, they can manually copy out the links.

Thanks!

# ChatGPT Said:

Worked for 1m 33s

- **Download:** Simple Bookmarks Manager v0.9.20
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `about.html`.
- **Changes:**

    - Converted the GitHub URL in About → Project from a hyperlink to plain selectable text.
    - Converted the Chrome Web Store URL to plain selectable text.
    - Both now display the full `https://` address for easy manual copying.
    - No clickable `<a>` elements remain in `about.html`.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 21 locale files parsed and were untouched; all 41 text files use LF; only the three expected files changed from v0.9.19; binary assets and permissions are unchanged; ZIP integrity and root structure passed.
- **SHA-256:** `187a98ce09c2b096ebf028b13c1fbbfea82e90e965d567b5e6171eb687082e23`

# User Said:

Forgot to request, please also expand the vertical space of the About window's default size so it will show the whole thing without needing to scroll. We'll name this update's file as v0.9.20.1 without changing the version number elsewhere (filename only).

I have not yet updated my repo.

Thanks!

# ChatGPT Said:

Worked for 1m 26s

- **Download:** Simple Bookmarks Manager v0.9.20.1
- **Files changed:** `manager.js`, `manager.css`.
- **Change:** About now uses a taller default dialog height of **760px**, while still respecting the available browser viewport. Help and Changelog retain their existing 620px default sizing.
- As requested:

    - `manifest.json` remains **0.9.20**.
    - `CHANGELOG.md` is unchanged.
    - Only the ZIP filename uses **v0.9.20.1**.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 41 text files use LF; binaries unchanged; only the two expected files differ from v0.9.20; ZIP integrity/root structure passed.
- **SHA-256:** `3fc5e7ad07c06b69a5fc0a42a32b4b302330dcd4caaf385601710c13b3adca34`

# User Said:

Made manual change to the about height. Uploading "Simple\_Bookmarks\_Manager\_v0.9.20.2.zip" for next time.

# ChatGPT Said:

Got it. I’ll use your uploaded `Simple_Bookmarks_Manager_v0.9.20.2.zip` as the starting point for the next SBM update, preserving your manual About-height change.
