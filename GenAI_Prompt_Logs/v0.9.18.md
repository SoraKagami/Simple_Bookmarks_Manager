# ChatGPT Said:

v0.9.17 Tested without issues, thanks!

I forgot my packaged releases do not contain the documentation folder (present on Github).

For v0.9.18:

- Please update the text for `Bookmarks_SingleClickOpen` (0) to "Disabled (Double Click to open Bookmarks)".
- Please add a new optional toggle under Options -> Search: "Launch SBM with focus in Search" (Var: "Launch\_SearchFocused" default=true). When `Launch_SearchFocused`=true, at SBM's launch/opening, the user input focus will be in the search text textbox. Please also generate an appropriate description for this new option.
- Please update help.html with an appropriate entry for the new `Launch_SearchFocused` feature.

Thanks!

# ChatGPT Said:

Worked for 2m 10s

- **Download:** Simple Bookmarks Manager v0.9.18
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `settings.js`, `options.html`, `manager.js`, `help.html`, `_locales/en/messages.json`.
- **Changes:**

    - Changed `Bookmarks_SingleClickOpen` mode `0` text to **Disabled (Double Click to open Bookmarks)**.
    - Added Search option **Launch SBM with focus in Search**.
    - Added `Launch_SearchFocused`, default `true`.
    - When enabled, SBM focuses and selects the **Search bookmarks** field immediately after the manager UI finishes its initial render.
    - Existing first-launch Help behavior remains accessible: if Help opens at launch, its modal takes focus afterward.
    - Added the corresponding Help entry.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 42 settings have matching Options controls; all 41 text files use LF; existing placeholders validated; binary assets are unchanged; permissions unchanged; ZIP integrity and root structure passed.
- **Locale caveat:** the three new v0.9.18 strings remain English-only as requested by your locale policy. Other locales use SBM's existing English fallback.
- **SHA-256:** `3eb2f9455371852a7783c17257532aedbd6497e158db5137ff23ac3fc0e1ad7d`

**Double-check:** test startup in both Tab Mode and Sidebar Mode with `Launch_SearchFocused` enabled/disabled, and confirm typing immediately after SBM opens enters text into Search.