# User Said:

v0.9.8: Thank you for the clarification. I miss-typed that. Testing went well.

For v0.9.9:

- New Feature: If selected text or URL is drag & dropped into SBM, SBM will attempt to add a new bookmark at that location & bring up the add new bookmark popup, with the selected text or URL pre-entered into the URL field. If the user drags in the selected text or URL and before letting go of the left mouse button, the user can control where the new entry will go in the bookmarks tree (either Library Pane or Middle Bookmarks pane), along with a preview indicator like when dragging bookmarks/separators/folders around. This feature should work irregardless of `SideBarMode` being enabled or disabled.
- Please create a new popup for adding a a new folder that is of the same style as when creating/adding a new bookmark. The current "Add New Folder" creates a default chromium popup that is out of style, and out of place. This should apply to all "Add New Folder" methods (Button, context menus etc.).
- Please change the max delay for "Folder\_AutoExpandAfterWait" to 3 seconds and change the data entry from a text field to a slider.

Thanks!

# ChatGPT Said:

Worked for 13m 7s

- **Download:** Simple Bookmarks Manager v0.9.9
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `manager.js`, `manager.css`, `settings.js`, `options.html`, `options.js`, `options.css`, `help.html`, `_locales/en/messages.json`.
- **Implemented:**

    - External selected text/URLs can now be dragged into Library or Folder Contents, with SBM’s existing before/inside/after drop indicators.
    - The **New Bookmark** popup opens with the dropped value pre-entered in the URL field; the bookmark is created only after **Create** is confirmed.
    - URL drags prefer `text/uri-list`, with `text/plain` fallback. [![](https://www.google.com/s2/favicons?domain=https://developer.mozilla.org&sz=128)MDN Web Docs+1](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer/getData?utm_source=chatgpt.com)
    - Empty Folder Contents space accepts a drop by appending to the current folder.
    - Replaced the native **Add New Folder** prompt with an SBM-styled modal for toolbar and context-menu creation paths.
    - `Folder_AutoExpandAfterWait` is now a **0.1–3.0 second slider**, retaining the `1.2` second default.
    - No permissions were added or changed. Bookmark creation continues through `chrome.bookmarks.create()` with validated parent/index/URL data. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)Chrome for Developers](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?hl=en&utm_source=chatgpt.com)
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 21 locale JSON files parsed; placeholder validation passed; all 41 text files use LF; binary assets are byte-for-byte unchanged; ZIP integrity/root structure passed.
- **Locale caveat:** only English was touched. The existing 20 recent English-only keys remain intentionally absent from other locales and continue using SBM’s established English fallback.
- **Behavior caveat:** precise before/after external drops in the middle pane remain unavailable while Search or a non-default Visual Sort is active, matching SBM’s existing protection against ambiguous native bookmark ordering. Dropped plain text that is not a valid URL is still prefilled, but SBM’s existing URL validation requires it to be corrected before creation.
- **Cross-browser:** this drag/drop path uses standard HTML Drag and Drop plus Chromium’s bookmarks API, so Chrome/Chromium/Brave/Edge should behave equivalently. [HTML DataTransfer documentation](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer/getData?utm_source=chatgpt.com) · [Chrome Bookmarks API](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?utm_source=chatgpt.com)

**Double-check:** manually test dragging selected webpage text, hyperlinks, and address-bar URLs into both panes, including before/inside/after folder targets and an empty Folder Contents pane.

SHA-256: `580bb164899c60ab02f8b84f649fe438e6ac32bf45c92f3ba6c83a3020a4943b`