# User Said:

v0.9.3 went well, thank you. There were a few issues that needs addressing.

For v0.9.4:

- Please use my original `EyeOpen.png` and `EyeClosed.png`. I did not ask for them to be changed to something else. Let me know if I need to re-upload.
- When `SideBarMode` is enabled, can you please change the opening of the Options behavior so that it will open `options.html` in a new tab & switch focus to the newly opened options tab instead of in the sidebar (too small)? This behavior should revert to the current one when `SideBarMode` is disabled.
- When `SideBarMode` is enabled, can you add code so that the current SBM tab closes while SBM is opened in the sidebar?
- No need to change About, Help, or Changelog menu behaviors. They look ok in the side panel/sidebar.

Thanks!

# ChatGPT Said:

Worked for 4m 2s

- **Download:** Simple Bookmarks Manager v0.9.4
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `manager.js`, `icons/EyeOpen.png`, `icons/EyeClosed.png`.
- **Changes:**

    - Restored your original 64×64 eye images byte-for-byte.
    - Sidebar Mode now opens `options.html` in a new active tab.
    - When the sidebar manager opens, the currently active registered SBM manager tab closes.
    - Unrelated tabs and inactive SBM instances are not closed.
    - Options retains its embedded-dialog behavior when Sidebar Mode is disabled.
    - About, Help, and Changelog behavior is unchanged.
- The side-panel context is distinguished using `chrome.tabs.getCurrent()`, which returns `undefined` outside a tab. `tabs.create()`, `tabs.query()`, and tab manipulation do not require adding the broad `tabs` permission for this usage. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)Chrome for Developers+1](https://developer.chrome.com/docs/extensions/reference/api/tabs?utm_source=chatgpt.com)
- **Validation:** All 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 42 text files use LF; existing binaries were unchanged apart from the intentionally restored eye assets; ZIP integrity and root structure passed.
- **SHA-256:** `2e76629f8053ad87afdb973579d0ca8192dc070f26cf4d126d895fc43e5d53ea`

### Double-check

- Confirm Options opens and receives focus from the Brave sidebar.
- Confirm opening the sidebar while an SBM tab is active closes only that SBM tab.