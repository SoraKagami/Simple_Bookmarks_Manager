# User Said:

v0.9.5 went well, thank you.

For v0.9.6:

- If `SidebarMode` is enabled:
    - If "Start at chosen folder" is enabled and a "Startup folder" is set, the selected startup folder (if valid) will be auto expanded on SBM launch.
    - A new subcategory category should be added in the "User Interface" category, below the "Sidebar Mode" toggle called "Sidebar Mode Options". This subcategory should be collapsible.
    - In the "Sidebar Mode Options" subcategory, please add:
          - (Toggle) "Auto show bookmarks when folders expanded" (Var: `SidebarMode_AutoShowOnExpand`) with an appropriate description. When `SidebarMode_AutoShowOnExpand` is enabled, the show bookmarks toggle for that folder should be enabled for all expanded folders. When toggle is off, default behavior to not auto show (but leave any user enabled folders alone as is).
- Update help.html with a new entry for "Auto show bookmarks when folders expanded" under "User Interface"

Thanks!

# ChatGPT Said:

Worked for 4m 58s

- **Download:** Simple Bookmarks Manager v0.9.6
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `settings.js`, `options.html`, `manager.js`, `help.html`, `_locales/en/messages.json`.
- **Implemented:**

    - Added collapsible **Sidebar Mode Options** under User Interface.
    - Added `SidebarMode_AutoShowOnExpand`, default `false`.
    - When enabled, expanding a Sidebar Library folder automatically enables that folder’s bookmark eye toggle.
    - Turning the option off does **not** hide bookmarks already manually/automatically shown.
    - A valid configured startup folder is now automatically expanded when launching with `SidebarMode=true`.
    - Updated Help documentation.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 21 locale JSON files parsed; existing English placeholders/messages were preserved; all 42 text files use LF; binary assets were unchanged; ZIP integrity/root structure passed.
- **Locale caveat:** the 5 new strings are English-only by design, following your locale rules. Other locales use SBM’s existing English fallback.
- **References:** [Chrome Bookmarks API](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?utm_source=chatgpt.com) · [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/api/storage?utm_source=chatgpt.com)
- **SHA-256:** `c084f640cb0b43123392f38ef7b242a15d6a033f649db4612ced9f6a888d0ca4`

**Double-check:** test startup with a nested configured folder, expand/collapse folders with auto-show both enabled and disabled, and confirm manually enabled eye toggles remain shown after disabling the new option.