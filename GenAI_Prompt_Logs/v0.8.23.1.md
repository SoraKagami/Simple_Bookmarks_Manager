Thanks, that worked. Minor polish issue.

For v0.8.23.1 Can you please fix this issue: When a bookmark is edited via the "F2" shortcut, the focus should be on the "Bookmark name" text field instead of the "Bookmark URL" text field when using "Edit".

Thanks!

Thought for 25s

Done — I created **v0.8.23.1**.

Download:

Download simple\_bookmarks\_manager\_v0.8.23.1.zip

Changes:

- Updated F2 bookmark editing so **Edit Bookmark** focuses the **Bookmark name** field first.
- Kept normal context-menu **Edit** behavior unchanged.
- Updated `manifest.json` to `0.8.23.1`.
- Added a `## 0.8.23.1` entry to `CHANGELOG.md`.
- Kept text files normalized to LF.

Validation:

- `node --check` passed for all JavaScript files.
- All JSON files parse successfully.
- Confirmed LF line endings.
- ZIP integrity check passed.