# User Said:
Something did not work right. When the `Sidebar Mode` toggle was enabled, the extension did not request for the optional sidebar permission, and on next SBM startup, SBM fails to show.

Turns out `sidePanel` as an optional\_permission is still broken on the latest chromium builds? I manually changed the extension permission to include `sidePanel` and removed the optional permission, reloaded extension and Brave crashed. Next restart/reload, SBM can open in the sidebar now.

For v0.9.1:
- Please check if the optional permission for `sidePanel` was implemented correctly. If it was, let me know and let's change this to a permanent permission request & remove the optional permission & the code that requests for optional permissions for the `sidePanel`. If there was a bug, please let me know and we can try a fix if you deem it worth trying (seems unlikely from my manual check).

Thanks.

# ChatGPT Said:

Worked for 5m 42s

- **Download:** Simple Bookmarks Manager v0.9.1
- **Review result:** The v0.9.0 optional-permission implementation was structurally correct: it requested permission directly from the toggle event, checked the result, and disabled `SidebarMode` when unavailable. A visible prompt was not guaranteed because Chromium only prompts when a permission introduces a new warning. However, Chrome’s Side Panel documentation consistently declares `sidePanel` as a regular manifest permission, so the optional approach is not reliable enough for SBM. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)developer.chrome.com+1](https://developer.chrome.com/docs/extensions/reference/api/permissions?authuser=2)
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `options.js`, `service_worker.js`, `_locales/en/messages.json`.
- **Changes:**

    - Updated to `0.9.1`.
    - Moved `sidePanel` into permanent `permissions`.
    - Removed `optional_permissions`.
    - Removed all `permissions.request()`, `permissions.contains()`, permission-denial, and permission-removal handling.
    - Removed the additional Options startup reconciliation that could reset `SidebarMode`.
    - Updated the Sidebar Mode description.
    - Preserved `SidebarMode=false` and normal tab behavior by default.
- **Validation:** All 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 21 locale files parsed; LF-only line endings confirmed; binary files unchanged; ZIP integrity passed; files are at the ZIP root.
- **Locale note:** The two Sidebar Mode strings remain English-only by design. The other 18 Chromium locales continue using English fallback until translations are requested.

### Double-check

- Brave UI/sidebar behavior still requires manual testing; Brave is unavailable in this environment.
- Your reported Brave crash appears browser-side, so this release cannot guarantee that reloading after the required-permission change will not trigger the same Brave issue.
- SHA-256: `3dc62ab9673b6170f93983d224041767401870ead10213dca2e51763bfcc0224`