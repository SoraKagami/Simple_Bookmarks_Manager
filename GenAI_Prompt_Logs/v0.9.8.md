# User Said:

v0.9.7 went well, thank you again!

For v0.9.8

- New feature option under Advanced, below "Single Click Folder Expansion/Collapse": "Folder auto-expands when user-selection dragged over folder" or a better description if you can recommend one, and an appropriately named variable. This feature when enabled will auto expand a folder if a user selected (dragged) bookmark, separater or folder hovers above the folder after X seconds (Var: `Folder_AutoExpandAfterWait` default 1.2 seconds).
- New option below the above for variable `Folder_AutoExpandAfterWait` with an appropriate title and description. When `Folder_AutoExpandAfterWait`=false, this section will be disabled (but still visible) & the font colour should reflect being disabled (an appropriate shade of grey).
- New `help.html` entries for the above two options under "Advanced"

Thanks!

# ChatGPT Said:

Worked for 10m 27s

- **Download:** Simple Bookmarks Manager v0.9.8
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `settings.js`, `options.html`, `options.css`, `options.js`, `manager.js`, `help.html`, `_locales/en/messages.json`.
- **Implemented:**

    - Added **Auto-expand folders during drag** → `Folder_AutoExpandOnDrag`, default `false`.
    - Added **Auto-expand delay (seconds)** → `Folder_AutoExpandAfterWait`, default `1.2`.
    - Delay supports `0.1–30` seconds and is disabled/muted while auto-expand is off.
    - Library folders expand inline after the hover delay without replacing the whole tree during the active drag.
    - Folder Contents folders automatically open after the delay, allowing continued dragging into deeper folders.
    - Supports dragged bookmarks, separators, folders, and existing multi-selection drag behavior.
    - Added both Advanced Help entries.
- **Clarification applied:** I interpreted your `Folder_AutoExpandAfterWait=false` wording as referring to the new enable/disable toggle. `Folder_AutoExpandAfterWait` remains the numeric delay; `Folder_AutoExpandOnDrag=false` disables its UI row.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 21 locale JSON files parsed; existing placeholders were unchanged; the 6 new strings are intentionally English-only; all 41 text files use LF; binaries were unchanged; ZIP integrity/root structure passed; no permissions changed.
- **References:** Chromium's bookmark moves continue using `chrome.bookmarks.move()`. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)Chrome for Developers](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?hl=en&utm_source=chatgpt.com) The drag-hover timer follows normal HTML drag event behavior (`dragover`, `dragleave`, `drop`, `dragend`). [![](https://www.google.com/s2/favicons?domain=https://developer.mozilla.org&sz=128)MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/API/HTML_Drag_and_Drop_API/Drag_operations?utm_source=chatgpt.com)
- **SHA-256:** `c2072d40b2699d0c4b07aee8f8ae4e51a923ca2769a6a91f31ca92bae8c78ce3`

**Double-check:** test dragging through several nested folders in both Library and Folder Contents, cancellation after an auto-open, multi-selection drags, and changing the delay to shorter/longer values.