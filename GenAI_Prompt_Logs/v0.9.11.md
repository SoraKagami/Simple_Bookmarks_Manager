# User Said:

v0.9.10 had a bug/oversight.

For v0.9.11:

- Bug/oversight fix for when `Folder_AutoExpandOnDrag` is enabled: When a dragged item hovers over a folder in the Library pane and passes the `Folder_AutoExpandAfterWait` time, even if the target folder does not have descendent folders, the middle bookmarks pane should switch to that folder.
- Please fix a mistake of mine made in v0.9.5. The update you made "Changed the default middle-pane root label from **Bookmarks bar** to **Bookmarks**." needs to be reverted (just this part). I had mistaken that as the title label, when it was the actual folder named "Bookmarks Bar".

Thanks!

# ChatGPT Said:

Worked for 2m 27s

- **Download:** Simple Bookmarks Manager v0.9.11
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `manager.js`.
- **Fixes:**

    - With `Folder_AutoExpandOnDrag` enabled and `SidebarMode=false`, hovering a **leaf folder** in the Library for `Folder_AutoExpandAfterWait` now switches Folder Contents to that folder even though there is nothing in the tree to expand.
    - Reverted the v0.9.5 breadcrumb substitution. SBM now displays Chromium's actual folder title, e.g. **Bookmarks bar**, rather than replacing it with **Bookmarks**. Chromium exposes the displayed folder title through `BookmarkTreeNode.title`. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)Chrome for Developers](https://developer.chrome.com/docs/extensions/reference/api/bookmarks)
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all 41 text files use LF; locales and binary assets were untouched; permissions unchanged; ZIP integrity and root structure passed.
- **Reference:** [Chrome Bookmarks API](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?utm_source=chatgpt.com)

**Double-check:** test drag-hover over a Library folder containing bookmarks but no subfolders, and confirm both the root and nested Folder Contents breadcrumb paths now show the browser-provided **Bookmarks bar** title.

SHA-256: `35ed845b314224903365a64438871d817ada3fca280b60717ff1c4d7d0c44fd8`