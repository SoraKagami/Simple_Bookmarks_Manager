# User Said:

v0.9.9 Went well during testing. Thank you!

For v0.9.10:

- For the `Folder_AutoExpandOnDrag` feature, when the user drags and triggers it over a folder in the Library pane and `SideBarMode` is not enabled, please change the currently viewed folder in the middle bookmarks pane to the targeted folder from the Library pane. If the user triggers a folder change using this in the middle bookmarks pane instead, please reflect the new folder being selected in the Library pane; if the folder is a subfolder of a collapsed folder, please expand the tree to that folder in the Library pane too.

Thanks!

# ChatGPT Said:

Worked for 4m 52s

- **Download:** Simple Bookmarks Manager v0.9.10
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `manager.js`.
- **Implemented:**

    - With `Folder_AutoExpandOnDrag` enabled and `SidebarMode=false`, drag-hover expansion of a Library folder now changes Folder Contents to that folder.
    - Drag-hover navigation triggered from Folder Contents now selects the corresponding Library folder.
    - Collapsed Library ancestors are expanded incrementally so the selected target becomes visible.
    - The Library tree is not fully rebuilt during the active drag, reducing the chance of disrupting Chromium's native drag session.
    - Sidebar Mode's existing behavior remains unchanged.
- **Validation:** all 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all locale JSON files parsed; all text files use LF; binaries were unchanged; permissions were unchanged; ZIP integrity and root structure passed.
- **Caveat:** SBM's existing unsaved-Details protection remains in effect, so drag-hover navigation will not silently navigate away while Details contains unsaved edits.
- **Reference:** [Chrome Bookmarks API](https://developer.chrome.com/docs/extensions/reference/api/bookmarks?utm_source=chatgpt.com)

**Double-check:** test both directions with nested collapsed folders, including dragging from Library → Library and Folder Contents → Folder Contents/Library.

SHA-256: `17df21b285f13651d55bed0bcd00665771938431ad3c8ec482088ab3b1a5bbe4`