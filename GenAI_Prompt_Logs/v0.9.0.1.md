# User Said:
For v0.9.0, please implement the following:
- New toggle setting under options `Sidebar Mode` along with a variable `SidebarMode` (Default = false). This should be under the `User Interface` section, below Font spacing. The description should be something like: When enabled, SBM will switch from opening in a tab to opening in the browser's \*\*Sidebar\*\* (Browser Chromium sidebar support required). \*\*Additional Optional Extension Permission Required\*\*: `sidePanel`. For UI consistency, the `Sidebar Mode` toggle in this section should be displayed on the right, aligned with the dropdown menus above.
- `SidebarMode` when enabled will switch SBM to opening in inside Chromium's side panel instead of in a tab. This includes when SBM is launched from the extension's toolbar/action icon. When disabled (`SidebarMode`=false) preserve SBM's existing behavior.
- To facilitate the new `SidebarMode`, new optional permissions will required & added to the SBM extension: `sidePanel`.
- If `SidebarMode`=true, A check to see if the extension has been granted the optional permission `sidePanel`; If given, continue; if missing, request optional permission. If permission requested & not given, set `SidebarMode`=false & the UI option should reflect this. This check should be run when the option page's setting for `SidebarMode` is changed.
- For now we will use the `manager.html` for the side panel implementation and make changes later.

# ChatGPT Said:

Worked for 6m 11s

- **Download:** Simple Bookmarks Manager v0.9.0 ZIP
- **Files changed:** `manifest.json`, `CHANGELOG.md`, `settings.js`, `options.html`, `options.css`, `options.js`, `service_worker.js`, `_locales/en/messages.json`.
- **Changes:** Added `SidebarMode` with default `false`; added the right-aligned Options toggle below Font spacing; declared optional `sidePanel` permission; requests permission when enabling; restores the setting to `false` if denied, unavailable, or later removed.
- **Launch behavior:** When enabled and permitted, `manager.html` is configured with `chrome.sidePanel.setOptions()` and toolbar clicks use `setPanelBehavior({openPanelOnActionClick: true})`. When disabled, the existing tab and single/multiple-instance behavior remains active. [![](https://www.google.com/s2/favicons?domain=https://developer.chrome.com&sz=128)Chrome for Developers+1](https://developer.chrome.com/docs/extensions/reference/api/sidePanel)
- **Validation:** All 8 JavaScript files passed `node --check`; all 22 JSON files parsed; all locale JSON files parsed; LF-only line endings confirmed; binary assets unchanged; HTML IDs unique; ZIP integrity passed; extension files are at the ZIP root.
- **Locale caveat:** Only English received the three new strings, as requested. Other locales intentionally fall back to English until translations are requested.

**Double-check**

- Interactive toolbar opening and the Chromium permission prompt require manual testing in Chrome/Brave/Edge; the container’s headless Chromium could not complete a UI smoke test because its GPU process failed.
- SHA-256: `32a29083d742ea6767a4c6a6f6e4b6e4dce26a2a6d62db2ed463e9a4d452a1f6`