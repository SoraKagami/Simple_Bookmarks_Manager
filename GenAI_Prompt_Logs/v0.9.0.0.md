# User Said:

You are helping me continue development of my Chromium extension, **Simple Bookmarks Manager** / **SBM**.

SBM is a local bookmarks manager for Chromium browsers. It is a Manifest V3 extension and should work across Chromium-based browsers such as Brave, Chrome, Edge, Opera, etc.

Use the latest ZIP file I upload as the starting point or if no upload provided, use latest version's zip. Do not rely on older generated files unless I explicitly say to.

## General working rules

* Make only the requested changes.
* Avoid changing unrelated files.
* Always update `CHANGELOG.md` with a new version entry for the work done.
* Always update `manifest.json` to the requested version.
* Use **Semantic Versioning 2.0.0**
* Use the exact version number I request. If the versioning request is unclear, ask me before changing files.
* Keep text file line endings normalized to **LF**, including `.js`, `.css`, `.html`, `.json`, `.md`, etc.
* Keep binary files untouched.
* Package the final extension as a ready-to-test `.zip`, with the extension files at the ZIP root, not inside an extra parent folder.
* At the end, provide a download link to the ZIP.

## Validation to perform before returning a ZIP

Please validate:

* `node --check` passes for all JavaScript files.
* All JSON files parse successfully.
* Text files use LF line endings.
* ZIP integrity check passes.
* If locale files were touched:

  * all locale JSON files parse successfully;
  * locale key coverage matches `_locales/en/messages.json`, unless there is a deliberate reason not to;
  * placeholders such as `{error}`, `{action}`, `$name$`, etc. are preserved exactly.

## Summary format

In the final response, please include:

* Download link to the ZIP.
* Files changed.
* Summary of changes.
* Validation performed.
* Any caveats, skipped items, or things that require my decision.

## Locale and translation rules

Unless I explicitly ask for locale updates, do **not** touch locale files except:

* `_locales/en/messages.json`

For new UI text, add English strings only unless I request translations.

Locale locations:

* Chromium locales: `_locales/<locale>/messages.json`
* Custom/internal locales: `sbm_locales/<locale>/messages.json`

When updating translations:

* Use `_locales/en/messages.json` as the base.
* Preserve all placeholders exactly.
* Preserve JSON structure.
* Do not translate technical terms awkwardly if keeping English is clearer.
* For Chinese:

  * `zh_TW` must use **Traditional Chinese**.
  * `zh_CN` must use **Simplified Chinese**.
  * Take extra care not to mix character sets.
* For Māori:

  * If a proper Māori term is uncertain or inappropriate for UI context, keep English.
* For `en_Netspeak`:

  * This is used for visual locale debugging.
  * It may be playful and funny, but must stay appropriate.
  * It should be visually distinct from normal English.
  * Safe humor and emojis are acceptable.
* In `i18n.js`, keep `en_Netspeak` at the bottom of the language list.

## Permissions and privacy rules

Do **not** add new permissions to `manifest.json` unless I explicitly approve first.

Current expected permissions should remain limited to the existing set, such as:

* `bookmarks`
* `favicon`
* `storage`

Do not add permissions such as `tabs`, `activeTab`, `history`, `downloads`, host permissions, or optional permissions without asking me first.

SBM must remain privacy-focused and local:

* No telemetry.
* No analytics.
* No ads.
* No subscriptions.
* No remote code.
* No remote assets.
* No network calls unless I explicitly request and approve the change.

## Security rules

When coding, keep extension attack surface low.

Prefer:

* `textContent` over `innerHTML`.
* DOM node creation over HTML string injection.
* Safe URL handling.
* Centralized validation helpers for risky behavior.
* Fixed enums for settings values.
* Defensive parsing of `chrome.storage.local` values.
* No `eval`.
* No `new Function`.
* No remote script loading.
* No remote asset loading.
* No avoidable console warnings/errors for expected behavior.

If a warning/error is expected behavior, route it through SBM’s existing debug warning/error log where appropriate, or avoid logging it as a browser extension warning.

## Performance rules

Please keep performance in mind:

* Avoid unnecessary overlays or extra DOM layers.
* Avoid repeated DOM scans where a cached reference or existing state is enough.
* Use `DocumentFragment` or batched DOM updates when rendering many items.
* Avoid unnecessary work during startup.
* Avoid adding persistent memory usage for rare UI actions.
* If adding an optimization that might affect behavior, gate it behind an existing or new setting only if appropriate.

## UI and UX rules

* Preserve existing behavior unless the requested change requires modifying it.
* Avoid generic browser prompts for important edits when SBM already has dedicated popup/dialog UI.
* Popups with editable content should generally not close just because the user clicks outside them.
* For layout fixes, prefer simple CSS/layout changes over overlays or runtime drawing layers.
* If a request is vague or there are multiple reasonable interpretations, ask me for clarification before implementing.

## Documentation/comment rules

* Add documentation comments for new functions.
* Update stale comments if function behavior changes.
* Keep comments useful and concise.
* Document complex behavior, security-sensitive logic, settings normalization, rendering paths, and event handling.

## Current feature context to remember

SBM includes:

* Left Library folder tree.
* Middle Folder Contents pane.
* Right or bottom Details pane, controlled by `DetailsPanePosition`.
* Resizable panes.
* Bookmark/folder editing.
* Custom context menus.
* Search and sorting.
* Themes, including Soft Blue as the current default theme.
* About, Changelog, and Help hamburger-menu popups.
* Help can show once on first launch via `ShowHelpOnLaunch`.
* Existing debug warning/error log in Options.
* Locale support across `_locales` and `sbm_locales`.
* `en_US` locale option.
* `en_Netspeak` debug locale.

## If uncertain

If anything is unclear, risky, requires new permissions, may affect user privacy, may alter existing behavior significantly, or could be implemented in multiple ways, please ask me before making the change.

# ChatGPT Said:
I’ll use `Simple_Bookmarks_Manager_v0.8.32.zip` as the starting point.

Please provide:

- The exact changes to implement.
- The exact new Semantic Version number.

I’ll then update `CHANGELOG.md` and `manifest.json`, validate the extension, and return a ready-to-test ZIP with files at the ZIP root.
