# User Said:

v0.10.13 has an implementation bug: The warning is still being logged in console & shown using the browser's popup instead of the new custom themed text-only popup.

# User Said:

For v0.10.14 can you please investigate and fix this issue?
Thanks.

# ChatGPT Said:

Worked for 2m

The other bookmark url protection options appear to also be using the native chromium popup instead of the new themed one. They need addressing too, thanks.

Worked for 3m 30s

Done: Download `Simple_Bookmarks_Manager_v0.10.14.zip`

## Files changed

- `manifest.json`
- `CHANGELOG.md`
- `manager.js`

## Summary of changes

- Bumped version to `0.10.14`.
- Fixed remaining Bookmark URL Protection block paths that still used:

    - native Chromium/browser `alert()` popups;
    - browser console warnings.
- Covered direct bookmark open paths from:

    - Details pane open button;
    - row open/double-click behavior;
    - context-menu open actions;
    - folder open-all actions;
    - multi-select open-all actions.
- Protected URL blocks now use the SBM-themed text-only popup and transient SBM Warnings/Errors log.
- Context-menu open actions now keep raw bookmark URLs until the central open helper applies protection filtering, so blocked URLs are handled consistently.
- Permissions unchanged.

## Validation performed

- `node --check`: passed for all 8 JavaScript files.
- JSON parse: passed for all 22 JSON files.
- Locale JSON parse: passed for all 21 locale files.
- Placeholder preservation check: passed for shared locale keys.
- LF line endings: passed for all detected text files.
- Binary files unchanged.
- ZIP integrity: passed with `zipfile.testzip()` and `unzip -tqq`.
- ZIP structure: files are at ZIP root, no extra parent folder.

## Caveats / testing focus

- Non-English locale coverage gaps from v0.10.13 remain unchanged.
- No browser runtime test was performed here.
- Please test all enabled Bookmark URL Protection options with row open, Details open, context-menu open, folder open-all, and multi-select open-all.