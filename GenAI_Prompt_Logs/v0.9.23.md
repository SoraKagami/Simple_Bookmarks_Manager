# User Said:

v0.9.22 is still broken. The UI performance is still worse than v0.9.20.2. Is this related to the `queueBookmarkTextEffectRefresh()` and `clearBookmarkTextEffects()` calls that were added in multiple places that probably needs to be selectively enabled/disabled? Also the AutoScroll & text truncation options still do not work at all. I am unable to even test it.

The places where AutoScroll and Truncate should apply for the entry under the mouse cursor, when enabled should be:

- Sidebar mode
- Normal Mode (Sidebar mode off):
    - Library pane (left)
    - Middle pane's "Name" and "URL" columns. These should each respectively use their column width to determine whether to autoscroll or truncate. For truncate in the middle bookmarks pane, the row column width should be enough without needing to use other methods to determine displayed width.

The underlying cause for autoscroll and truncate not being applied when enabled has not been found and addressed yet.

For v0.9.23 please fix the above, thank you.

ChatGPT Said:

Worked for 6m 4s

- **Download:** Simple Bookmarks Manager v0.9.23

### Files changed

`manifest.json`, `CHANGELOG.md`, `manager.js`, `manager.css`.

### What changed

- Removed the v0.9.22 queued refresh behavior from normal selection/highlight paths.
- `Bookmark_TextTruncation = 0` now avoids the text-effect path during:

    - selection highlight updates;
    - normal render refreshes;
    - resize handling;
    - hover handling.
- Replaced the previous `requestAnimationFrame`-first text-effect model with immediate delegated `mouseover` / `mouseout` handling.
- Text-effect listeners exist only when `Bookmark_TextTruncation` is `1` or `2`.
- Effects now apply only to the row currently under the mouse cursor.
- Removed clicked-row persistence from this feature to reduce state and avoid extra refresh work.

### Fixed targets

I validated the new hover path against:

- Sidebar Mode Library rows.
- Normal Mode Library rows.
- Middle pane **Name** column.
- Middle pane **URL** column.

For the middle pane, Name and URL use their own visible column width when calculating scroll/truncation.

### Validation

- All 8 JavaScript files passed `node --check`.
- All 22 JSON files parsed successfully.
- All 21 locale JSON files parsed successfully.
- All 42 text files use LF line endings.
- HTML duplicate-ID check passed.
- Binary assets unchanged.
- Permissions unchanged.
- ZIP integrity/root structure passed.

### Extra browser harness check

I also ran a targeted Chromium DOM harness for the revised hover-effect logic:

- Normal Library + Truncate: passed.
- Middle Name + Truncate: passed.
- Middle URL + Truncate: passed.
- Sidebar Library + Truncate: passed.
- Middle URL + Auto Scroll: passed.
- Sidebar Library + Auto Scroll: passed.
- Switching back to mode `0`: listeners disabled and active effect elements cleared.

SHA-256: `0ccd454769187bb77670ee1d5ab9d7a224d794b09cae4a00668f6a82eff6ef5f`